import 'package:flutter/material.dart';

class Deteksi extends StatelessWidget {
  const Deteksi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Deteksi'),
      ),
      body: const Center(
        child: Text('Halaman Deteksi'),
      ),
    );
  }
}
