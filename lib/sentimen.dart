import 'package:flutter/material.dart';

class Sentimen extends StatelessWidget {
  const Sentimen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
